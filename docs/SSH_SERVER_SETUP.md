# SSH подключение к серверу 193.108.115.170

## ✅ Настройка завершена

SSH подключение по ключу к серверу **193.108.115.170** успешно настроено!

### 📋 Что было сделано:

1. **Создан SSH ключ** (ed25519): `~/.ssh/auto_server_193_108`
2. **Публичный ключ скопирован** на сервер в `~/.ssh/authorized_keys`
3. **Настроен SSH config** для удобного подключения

### 🚀 Как подключаться

Теперь вы можете подключаться к серверу **без ввода пароля**:

```bash
# Простое подключение через алиас
ssh auto-server

# Или напрямую по IP (тоже без пароля)
ssh -i ~/.ssh/auto_server_193_108 root@193.108.115.170
```

### 📝 Информация о сервере

- **IP**: 193.108.115.170
- **User**: root
- **Hostname**: 6090807-al360693
- **OS**: Ubuntu 24.04.3 LTS (Noble Numbat)
- **Kernel**: 6.8.0-88-generic

### 🔧 SSH Config

Конфигурация добавлена в `~/.ssh/config`:

```
Host auto-server
    HostName 193.108.115.170
    User root
    IdentityFile ~/.ssh/auto_server_193_108
    IdentitiesOnly yes
    StrictHostKeyChecking no
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

### 💡 Примеры использования

```bash
# Подключиться к серверу
ssh auto-server

# Выполнить команду на сервере
ssh auto-server "df -h"

# Скопировать файл на сервер
scp myfile.txt auto-server:/root/

# Скопировать файл с сервера
scp auto-server:/root/file.txt ./

# Скопировать папку на сервер
scp -r myfolder/ auto-server:/root/

# SSH туннель (например, для базы данных)
ssh -L 5432:localhost:5432 auto-server
```

### 🔐 Безопасность

- ✅ Приватный ключ: `~/.ssh/auto_server_193_108` (держите в секрете!)
- ✅ Публичный ключ: `~/.ssh/auto_server_193_108.pub`
- ✅ Ключ защищен правами доступа 600
- ⚠️ Старый пароль больше не нужен, но храните его на случай восстановления доступа

### 📦 Использование в проектах

Для использования в проектах (например, деплой):

```bash
# В скриптах деплоя
ssh auto-server "cd /path/to/project && git pull"

# В Docker Compose (для удаленного деплоя)
DOCKER_HOST="ssh://auto-server" docker-compose up -d

# В Ansible
ansible-playbook -i "auto-server," playbook.yml
```

### 🔄 Если нужно переподключить ключ

Если по какой-то причине подключение перестало работать:

```bash
# Пересоздать и скопировать ключ
ssh-keygen -t ed25519 -f ~/.ssh/auto_server_193_108 -C "auto_server" -N ""
sshpass -p 'cLK5ekyVNsS5Q#' ssh-copy-id -i ~/.ssh/auto_server_193_108.pub root@193.108.115.170
```

---

**Дата настройки**: 23 ноября 2024
**Тип ключа**: ED25519 (современный и безопасный)
**Fingerprint**: SHA256:KiqGezWCtZriX2o3UWM9a7IHLq32wCz/ddrFS0w+l5o
